import { Dimensions } from "react-native";

export const screenHeight = Dimensions.get("window").height;

//there might wanna be a change in fonts, using the below, it will be easy to change
export const FontBold = "SF_Bold";
export const FontMedium = "SF_Medium";
export const FontRegular = "SF_Regular";
